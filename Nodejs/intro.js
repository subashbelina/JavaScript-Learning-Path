/*
  What is Node.js?
  ----------------
  Node.js lets you run JavaScript outside the browser — on servers, scripts, and tools.

  Key ideas:
    - Built on Chrome's V8 JavaScript engine
    - Event-driven and non-blocking I/O
    - npm = package manager for Node modules
    - Great for APIs, CLI tools, real-time apps, and backend services

  Note: Read and learn only. Open each file to study Node.js syntax and patterns.
  Optional: run this file with  node Nodejs/intro.js
*/

console.log("--- What is Node.js? ---");
console.log("Node.js = JavaScript runtime for server-side and tooling");
console.log("");
console.log("Main concepts:");
console.log("  1. CommonJS modules  — require() / module.exports");
console.log("  2. Built-in modules  — fs, path, http, events, os…");
console.log("  3. npm               — install and manage packages");
console.log("  4. Event loop        — async, non-blocking I/O");
console.log("  5. process           — env vars, argv, exit codes");
console.log("");
console.log("Topics in this folder:");
console.log("  commonjs-modules.js  → require & module.exports");
console.log("  file-system.js       → read/write files (fs)");
console.log("  path-module.js       → join, resolve, basename");
console.log("  http-server.js       → create HTTP server");
console.log("  events-emitter.js    → EventEmitter pattern");
console.log("  streams.js           → readable & writable streams");
console.log("  process-env.js       → process.env & argv");
console.log("  os-module.js         → platform, CPU, memory");
console.log("  url-module.js        → parse & format URLs");
console.log("  crypto-basics.js     → hash & random bytes");
console.log("  buffer.js            → binary data in Node");
console.log("  async-patterns.js    → callbacks, promises in Node");
console.log("  error-handling.js    → Node-style errors");
console.log("  child-process.js     → run shell commands");
console.log("  npm-basics.js        → package.json & scripts");
console.log("  es-modules.js        → import/export in Node");
