/*
  What is Node.js?
  ----------------
  Node.js runs JavaScript outside the browser — on servers, scripts, and tools.
  Same language as the browser, different APIs (fs, http, process instead of DOM).
*/

console.log("--- What is Node.js? ---");
console.log("Node.js = JavaScript runtime for server-side and tooling");
console.log("");
console.log("Browser has: document, window, DOM");
console.log("Node has:    fs, path, http, process, npm");
console.log("");
console.log("Read folder 05 files in order — see README.md");
