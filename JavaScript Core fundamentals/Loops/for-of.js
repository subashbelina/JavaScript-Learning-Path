/*
  for...of Loop
  -------------
  Loop over iterable values (arrays, strings, etc.).
*/

console.log("--- for...of Loop ---");

const colors = ["Red", "Green", "Blue"];
for (const color of colors) {
  console.log(color);
}

const word = "JS";
for (const letter of word) {
  console.log(letter);
}
