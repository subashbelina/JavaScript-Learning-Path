/*
  What is Next.js?
  ----------------
  Next.js is a React framework for building full-stack web applications.

  Key ideas:
    - Built on top of React
    - File-based routing (folders = routes)
    - Server-side rendering (SSR) and static generation
    - API routes built in
    - Fast performance out of the box

  Note: Read and learn only. Create a real project later with:
    npx create-next-app@latest
*/

console.log("--- What is Next.js? ---");
console.log("Next.js = React framework for production web apps");
console.log("");
console.log("Main features:");
console.log("  1. File-based routing     — app/about/page.js → /about");
console.log("  2. Server Components      — render on server");
console.log("  3. Client Components      — interactivity in browser");
console.log("  4. Data fetching          — built into components");
console.log("  5. API routes             — backend in same project");
console.log("  6. SEO friendly           — metadata, SSR");
