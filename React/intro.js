/*
  What is React?
  --------------
  React is a JavaScript library for building user interfaces (UI).

  Key ideas:
    - Build UI with reusable components
    - Describe what the UI should look like (declarative)
    - React updates the page when data changes
    - JSX lets you write HTML-like syntax inside JavaScript

  Note: These files are for LEARNING syntax only.
  Read the examples below. To run React in a real app,
  use tools like Vite or Create React App later.
*/

console.log("--- What is React? ---");
console.log("React = JavaScript library for building UI");
console.log("");
console.log("Main concepts:");
console.log("  1. Components  — reusable UI pieces");
console.log("  2. JSX         — HTML-like syntax in JS");
console.log("  3. Props       — pass data to components");
console.log("  4. State       — data that can change");
console.log("  5. Hooks       — useState, useEffect, etc.");
console.log("");
console.log("See README.md in this folder for the full learning order.");
console.log("Next file → jsx.js");
