/*
  What is a Database?
  -------------------
  A database stores data permanently (users, posts, products).
  Your Express API reads and writes to a database instead of arrays in memory.
*/

console.log("--- What is a Database? ---");
console.log("Database = permanent storage for app data");
console.log("");
console.log("In memory (arrays)  →  data lost when server restarts");
console.log("Database            →  data saved to disk");
console.log("");
console.log("Two main types:");
console.log("  SQL (PostgreSQL, MySQL)     →  tables with rows and columns");
console.log("  NoSQL (MongoDB)             →  collections with JSON documents");
console.log("");
console.log("Read folder 07 files in order — see README.md");
